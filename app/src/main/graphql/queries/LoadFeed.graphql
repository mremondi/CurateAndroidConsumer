query LoadFeed($limit: Int, $userLocation: UserLocation) {
    # posts($userLocation: UserLocation, ...)
    posts(limit: $limit, userDistance: $userLocation) {
        ...PostsInfoFragment
    }
}

#TODO: In future, LoadFeed will take parameters UserLocation and UserID
