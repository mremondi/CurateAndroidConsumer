fragment MenuInfo on Item {
    menu {
        id
        name
        restaurant {
            id
            name
            stripeId
            isOpen
        }
    }
    menuSection {
        id
        section
    }
}
